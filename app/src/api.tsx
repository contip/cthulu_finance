import React from 'react';
import { IUserTransaction } from './interfaces';
import { Urls } from './constants';

// export default async function TradeApi(payload: IUserTransaction) {
//     let response: Response = await fetch(Urls[payload.type])


// }